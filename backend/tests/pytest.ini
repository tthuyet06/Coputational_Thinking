[pytest]
# Đi ngược lên 2 cấp (từ backend/tests -> backend -> Project) để lấy Project Root
pythonpath = ../..

# Tắt các cảnh báo (warning)
filterwarnings =
    ignore::pydantic.warnings.PydanticDeprecatedSince20
    ignore::DeprecationWarning

#Lệnh chạy code: pytest backend/tests